# 황수지 포트폴리오

React, TypeScript, Vite로 구성한 포트폴리오 웹사이트입니다. 화면에 표시되는 텍스트와 프로젝트 데이터는 루트의 `content.json`에서 관리합니다.

## 실행

```bash
npm install
npm run dev
```

## 검증

```bash
npm run typecheck
npm run build
```

이미지는 `public/assets`에 추가하고 `content.json`에서 `/assets/파일명` 또는 `./assets/파일명`으로 참조합니다.
